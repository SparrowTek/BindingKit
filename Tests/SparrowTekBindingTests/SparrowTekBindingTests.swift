import XCTest
@testable import SparrowTekBinding

final class SparrowTekBindingTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SparrowTekBinding().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
